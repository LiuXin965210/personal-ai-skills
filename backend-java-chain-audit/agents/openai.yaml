interface:
  display_name: "Java 后端链路梳理"
  short_description: "扫描 Java 调用链，支持完整报告和单行精简总览"
  default_prompt: "使用 $backend-java-chain-audit 扫描 Java 后端接口或 MQ 链路，默认输出精简总览：MySQL 按写入和只读区分、Redis 只显示 Key、外围接口包含完整服务名和路径；需要时再显式开启完整模式。"

policy:
  allow_implicit_invocation: true
