# {需求名称}后端设计

## 1. 变更记录

| 变更人 | 变更内容 | 变更时间 |
| --- | --- | --- |
| {姓名} | {新建/完善模板内容/调整说明} | {YYYY-MM-DD} |

## 2. 需求链接

{需求名称或需求链接}

## 3. 外围相关接口

### 1. {接口用途}

- 工程：`{工程名}`
- 接口 URL：`{接口路径或完整 URL}`
- 请求方式：`GET/POST/PUT/DELETE`

#### 接口入参

| 字段 | 名称 | 是否必须 | 类型 | 备注 |
| --- | --- | --- | --- | --- |
| `{field}` | {名称} | 是/否 | `{Type}` | {说明} |

#### 接口出参

| 字段 | 名称 | 是否必须 | 类型 | 备注 |
| --- | --- | --- | --- | --- |
| `{field}` | {名称} | 是/否 | `{Type}` | {说明} |

## 4. 接口设计

### 1. {接口名称}

- 接口 URL：`{接口路径}`
- 请求方式：`GET/POST/PUT/DELETE`
- 调用方：{前端/后端/定时任务/消费者}

#### 接口入参

| 字段 | 名称 | 是否必须 | 类型 | 备注 |
| --- | --- | --- | --- | --- |
| `{field}` | {名称} | 是/否 | `{Type}` | {说明} |

#### 接口出参

| 字段 | 名称 | 是否必须 | 类型 | 备注 |
| --- | --- | --- | --- | --- |
| `{field}` | {名称} | 是/否 | `{Type}` | {说明} |

## 5. Kafka 设计

### 1. {Consumer/Producer 名称}

- Topic：`{topic}`
- 工程：`{工程名}`
- 调整类型：新增/调整/本期无调整

#### 方法调整

1. {逻辑调整点}
2. {逻辑调整点}

## 6. Service 设计

### 1. `{类全名}#{方法名}`

#### 方法逻辑调整

1. {逻辑步骤}
2. {逻辑步骤}
3. {异常/边界处理}

## 7. 数据模型

### 1. 模型设计

{ER 图、表结构、Java 对象、Redis/ES/Mongo/Kafka 数据结构说明；不涉及时写本期无调整。}

```sql
-- 建表或字段变更 SQL，若无则写本期无调整
```

### 2. 配置项

| 配置项 | 值 | 说明 |
| --- | --- | --- |
| `{key}` | `{value}` | {说明} |

## 8. 增量变更脚本

```sql
-- alter table / insert config / update enum 等脚本
```

## 9. 待确认事项

1. {无法从需求或工程确认的点}
2. {需要产品/后端/测试确认的点}
